<!DOCTYPE html>
<html>

<head>
  <title>BrainSpill - Share Your Secrets</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Gloria+Hallelujah&family=Titan+One&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="css/styles.css">
</head>

<body>
  <% if (locals.authenticated) { %>
    <a href="/logout" class="btn logout-btn">Logout</a>
  <% } %>
  
  <h1 title="What's your secret?">What's your secret?</h1>
  
  <% if (locals.secret) { %>
    <div class="card">
      <p><%= locals.secret %></p>
    </div>
    <p class="user">- <%= locals.user %></p>
  <% } %>
  
  <% if (locals.authenticated) { %>
    <div class="button-container">
      <a href="/random" class="btn">Show Another Secret</a>
      <a href="/submit" class="btn">Submit My Secret</a>
    </div>
  <% } %>
</body>

</html>